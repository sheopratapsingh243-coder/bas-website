:root{
  --brand:#0b5aa6;
  --brand-dark:#083a6c;
  --accent:#f58b1f;
  --accent-dark:#d96c00;
  --fg:#0f172a;
  --muted:#475569;
  --bg:#f8fbff;
  --card:#ffffff;
  --line:#dbe6f2;
  --success:#0f9d58;
  --shadow:0 18px 55px rgba(15,23,42,.10);
  --radius:22px;
  --max:1180px;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
  color:var(--fg);
  background:linear-gradient(180deg,#fafdff 0%,#f6f9fc 100%);
  overflow-x:hidden;
}
a{color:inherit}
img{max-width:100%;display:block}
.wrap{max-width:var(--max);margin:auto;padding:0 18px}
.section{padding:72px 0}
.section.tight{padding:56px 0}
.section-title{font-size:clamp(30px,4vw,44px);line-height:1.1;margin:0 0 14px;font-weight:900;color:var(--brand-dark)}
.section-sub{font-size:17px;line-height:1.8;color:var(--muted);max-width:820px;margin:0}
.kicker{display:inline-flex;align-items:center;gap:8px;padding:10px 14px;border-radius:999px;background:rgba(11,90,166,.08);border:1px solid rgba(11,90,166,.14);color:var(--brand);font-size:13px;font-weight:900;letter-spacing:.2px}
.grid-2,.grid-3,.grid-4{display:grid;gap:22px}
.grid-2{grid-template-columns:repeat(2,minmax(0,1fr))}
.grid-3{grid-template-columns:repeat(3,minmax(0,1fr))}
.grid-4{grid-template-columns:repeat(4,minmax(0,1fr))}
@media(max-width:980px){.grid-2,.grid-3,.grid-4{grid-template-columns:1fr}}
.card{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow);padding:26px}
.soft-card{background:linear-gradient(180deg,#ffffff 0%,#fbfdff 100%);border:1px solid var(--line);border-radius:var(--radius);padding:24px}
.btn-row{display:flex;gap:14px;flex-wrap:wrap;margin-top:22px}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:10px;padding:14px 22px;border-radius:999px;text-decoration:none;font-weight:900;border:1px solid var(--line);transition:.25s transform,.25s box-shadow,.25s background,.25s color;cursor:pointer}
.btn:hover{transform:translateY(-2px);box-shadow:0 10px 26px rgba(15,23,42,.14)}
.btn-primary{background:linear-gradient(135deg,var(--accent),var(--accent-dark));color:#fff;border:none}
.btn-secondary{background:linear-gradient(135deg,var(--brand),var(--brand-dark));color:#fff;border:none}
.btn-light{background:#fff;color:var(--brand-dark)}
.hero{padding:78px 0 34px;position:relative;overflow:hidden}
.hero::before{content:"";position:absolute;inset:0;background:radial-gradient(circle at top right,rgba(245,139,31,.16),transparent 28%),radial-gradient(circle at top left,rgba(11,90,166,.14),transparent 30%);pointer-events:none}
.hero-grid{display:grid;grid-template-columns:1.08fr .92fr;gap:28px;align-items:center}
@media(max-width:980px){.hero-grid{grid-template-columns:1fr}}
.hero h1{font-size:clamp(36px,5vw,62px);line-height:1.03;letter-spacing:-.02em;margin:16px 0 16px;color:var(--brand-dark)}
.hero p{font-size:18px;line-height:1.8;color:var(--muted);margin:0;max-width:760px}
.hero-side{position:relative}
.hero-panel{background:linear-gradient(145deg,#ffffff 0%,#f5f9ff 100%);border:1px solid var(--line);border-radius:30px;box-shadow:var(--shadow);padding:22px}
.hero-image{aspect-ratio:4/3;border-radius:24px;overflow:hidden;position:relative;background:#dbeafe}
.hero-image img{width:100%;height:100%;object-fit:cover}
.hero-badge{position:absolute;left:16px;bottom:16px;background:rgba(15,23,42,.85);color:#fff;padding:12px 14px;border-radius:16px;max-width:78%;font-size:14px;line-height:1.5}
.stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:16px;margin-top:28px}
@media(max-width:980px){.stats{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:540px){.stats{grid-template-columns:1fr}}
.stat{background:#fff;border:1px solid var(--line);border-radius:22px;padding:22px;text-align:center;box-shadow:var(--shadow)}
.stat strong{display:block;font-size:34px;color:var(--brand);margin-bottom:8px}
.stat span{display:block;color:var(--muted);font-weight:700;line-height:1.6}
.value-list,.check-list,.steps{list-style:none;padding:0;margin:18px 0 0;display:grid;gap:12px}
.value-list li,.check-list li,.steps li{padding:14px 16px 14px 42px;position:relative;border:1px solid var(--line);border-radius:16px;background:#fff;color:#334155;line-height:1.7;font-weight:700}
.value-list li::before,.check-list li::before,.steps li::before{content:"✓";position:absolute;left:16px;top:12px;color:var(--brand);font-weight:900}
.impact-band{margin-top:26px;padding:18px 20px;border-radius:20px;background:linear-gradient(135deg,rgba(11,90,166,.08),rgba(245,139,31,.09));border:1px solid rgba(11,90,166,.12);font-weight:800;color:#1e293b;line-height:1.8}
.highlight{background:linear-gradient(180deg,#fff 0%,#f8fbff 100%);border:1px solid var(--line);border-radius:28px;padding:28px;box-shadow:var(--shadow)}
.mission-grid .card{height:100%}
.program-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:22px}
@media(max-width:1080px){.program-grid{grid-template-columns:1fr 1fr}}
@media(max-width:680px){.program-grid{grid-template-columns:1fr}}
.program-card{background:#fff;border:1px solid var(--line);border-radius:28px;overflow:hidden;box-shadow:var(--shadow);display:flex;flex-direction:column}
.program-card img{width:100%;aspect-ratio:16/10;object-fit:cover;background:#e2e8f0}
.program-card .body{padding:24px;display:flex;flex-direction:column;gap:14px;flex:1}
.program-tag{display:inline-flex;align-self:flex-start;padding:8px 12px;border-radius:999px;background:rgba(11,90,166,.08);border:1px solid rgba(11,90,166,.12);color:var(--brand);font-size:12px;font-weight:900;text-transform:uppercase;letter-spacing:.4px}
.program-card h3{margin:0;font-size:24px;color:var(--brand-dark)}
.program-card p{margin:0;color:var(--muted);line-height:1.8}
.program-metrics{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:2px}
.program-metrics div{background:#f8fbff;border:1px solid var(--line);border-radius:16px;padding:12px}
.program-metrics strong{display:block;color:var(--brand);font-size:22px}
.program-metrics span{display:block;color:var(--muted);font-size:13px;font-weight:700;line-height:1.5}
.doc-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:22px}
@media(max-width:980px){.doc-grid{grid-template-columns:1fr}}
.doc-card{display:flex;flex-direction:column;gap:16px;background:#fff;border:1px solid var(--line);border-radius:24px;padding:24px;box-shadow:var(--shadow)}
.doc-card h3{margin:0;font-size:22px;color:var(--brand-dark)}
.doc-card p{margin:0;color:var(--muted);line-height:1.75}
.doc-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:auto}
.info-grid{display:grid;grid-template-columns:1fr 1fr;gap:22px}
@media(max-width:980px){.info-grid{grid-template-columns:1fr}}
.kv-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:18px}
@media(max-width:640px){.kv-grid{grid-template-columns:1fr}}
.kv{padding:14px;border:1px solid var(--line);border-radius:16px;background:#fbfdff}
.kv b{display:block;font-size:13px;color:#64748b;margin-bottom:6px;text-transform:uppercase;letter-spacing:.4px}
.kv span{display:block;font-weight:800;color:#0f172a;line-height:1.6;word-break:break-word}
.notice{padding:16px 18px;border-radius:18px;background:rgba(245,139,31,.10);border:1px solid rgba(245,139,31,.18);color:#7c2d12;line-height:1.8;font-weight:700}
.contact-grid{display:grid;grid-template-columns:.95fr 1.05fr;gap:22px}
@media(max-width:980px){.contact-grid{grid-template-columns:1fr}}
.contact-card iframe{width:100%;min-height:360px;border:0;border-radius:22px}
.form-grid{display:grid;gap:14px}
.input,textarea{width:100%;padding:14px 16px;border-radius:16px;border:1px solid var(--line);font:inherit;background:#fff}
textarea{min-height:150px;resize:vertical}
.gallery-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:16px}
@media(max-width:980px){.gallery-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:560px){.gallery-grid{grid-template-columns:1fr}}
.gallery-grid img{width:100%;aspect-ratio:4/3;object-fit:cover;border-radius:20px;border:1px solid var(--line);box-shadow:var(--shadow)}
.cta-strip{padding:24px;border-radius:28px;background:linear-gradient(135deg,var(--brand),#0f4f92);color:#fff;display:grid;grid-template-columns:1.2fr .8fr;gap:18px;align-items:center;box-shadow:var(--shadow)}
@media(max-width:980px){.cta-strip{grid-template-columns:1fr}}
.cta-strip h3{margin:0 0 8px;font-size:30px}
.cta-strip p{margin:0;color:#e2e8f0;line-height:1.8}
.quote{padding:22px;border-radius:22px;background:linear-gradient(135deg,rgba(11,90,166,.08),rgba(245,139,31,.08));border:1px solid rgba(11,90,166,.14);font-size:18px;font-weight:800;line-height:1.8;color:#1e293b}
.topbar-space{height:92px}
.whatsapp-float,.donate-float{position:fixed;bottom:18px;z-index:1000;padding:14px 18px;border-radius:999px;color:#fff;text-decoration:none;font-weight:900;box-shadow:0 18px 45px rgba(15,23,42,.24)}
.whatsapp-float{right:18px;background:linear-gradient(135deg,#25D366,#128C7E)}
.donate-float{left:18px;background:linear-gradient(135deg,var(--accent),var(--accent-dark))}
@media(max-width:700px){.whatsapp-float,.donate-float{padding:12px 14px;font-size:14px}}
#globalHeader{position:fixed;top:0;left:0;width:100%;z-index:10000;background:rgba(255,255,255,.95);backdrop-filter:blur(10px);border-bottom:1px solid rgba(219,230,242,.95)}
#globalHeader .wrap{max-width:var(--max);margin:auto;padding:0 18px;display:flex;align-items:center;gap:18px;min-height:92px}
.brand{display:flex;align-items:center;gap:14px;text-decoration:none;min-width:0}
.brand-logo{width:62px;height:62px;border-radius:50%;object-fit:cover;border:2px solid rgba(11,90,166,.12)}
.brand-copy{display:flex;flex-direction:column;min-width:0}
.brand-copy b{font-size:18px;color:#0f172a;line-height:1.2}
.brand-copy span{font-size:13px;color:var(--brand);font-weight:700;line-height:1.45}
.nav{display:flex;align-items:center;gap:20px;margin-left:auto}
.nav a{font-weight:800;color:#334155;text-decoration:none;position:relative}
.nav a.active,.nav a:hover{color:var(--brand)}
.nav a.active::after,.nav a:hover::after{content:"";position:absolute;left:0;right:0;bottom:-8px;height:3px;border-radius:3px;background:linear-gradient(90deg,var(--brand),var(--accent))}
.header-actions{display:flex;align-items:center;gap:12px}
.lang-toggle,.menu-toggle{border:none;background:#eaf2fb;color:var(--brand);font-weight:900;border-radius:999px;padding:12px 16px;cursor:pointer}
.menu-toggle{display:none}
@media(max-width:980px){
  .menu-toggle{display:inline-flex}
  .nav{position:fixed;left:16px;right:16px;top:96px;background:#fff;border:1px solid var(--line);box-shadow:var(--shadow);border-radius:24px;padding:18px;display:none;flex-direction:column;align-items:flex-start;gap:16px}
  .nav.open{display:flex}
  .header-actions .btn{display:none}
}
#globalFooter{background:#0f172a;color:#fff;margin-top:72px;padding:58px 0 22px}
.footer-grid{display:grid;grid-template-columns:1.15fr .8fr .8fr .9fr;gap:26px}
@media(max-width:980px){.footer-grid{grid-template-columns:1fr}}
#globalFooter h3,#globalFooter h4{margin:0 0 12px}
#globalFooter p,#globalFooter a{color:#cbd5e1;line-height:1.8;text-decoration:none;margin:0 0 8px}
#globalFooter a:hover{color:#fff}
.footer-mini{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:999px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.08);font-size:13px;font-weight:800;color:#fff}
.footer-bottom{border-top:1px solid rgba(255,255,255,.12);margin-top:28px;padding-top:18px;text-align:center;color:#94a3b8}
.small{font-size:14px}
.text-center{text-align:center}
.hidden{display:none!important}
